import { TakimByProjePipe } from './takim-by-proje.pipe';

describe('TakimByProjePipe', () => {
  it('create an instance', () => {
    const pipe = new TakimByProjePipe();
    expect(pipe).toBeTruthy();
  });
});
